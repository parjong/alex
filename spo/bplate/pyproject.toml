[tool.poetry]
name = "bplate"
version = "0.1.0"
description = ""
authors = ["Jonghyun Park <parjong@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pylint = "2.13.9"

[tool.poetry.scripts]
bplate-inject = 'bplate.cli.inject:run'

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
