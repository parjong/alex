#include <native/toolchain/compile/gcc/instance.hpp>

#include <pepper/logging.hpp>

namespace native { namespace toolchain { namespace compile { namespace gcc {

  void instance::run(void) const
  {
    static pepper::logging::logger log{ "pepperbuild.native.toolchain.compile.gcc.instance.run" };

    log.warn( "not implemented, yet" );

    return;
  }

} } } }
